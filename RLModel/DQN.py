class DQN(object):
    def __init__(self, config, environment, exploration_strategy):
        # Exploration Strategy 초기화s
        # Environment 초기화

        # ReplayMemory 초기화

        # Q, Q hat 초기화
        pass
    
    def learn_episodes(self, episode_count):
        # for episode=1,M do
            # for t=1,T do
                # Action 선택
                # Action 실행
                # x_t+1 전처리
                # Transition ReplayMemory에 저장
                # Minibatch 추출, 학습
                # Q hat = Q

        pass